:root {
  --bg:#0d0816;
  --panel:#1b1229;
  --panel-2:#281a3a;
  --gold:#f5c76c;
  --gold-dark:#cc8c2d;
  --cream:#fff1d3;
  --text:#f7f4ff;
  --muted:#aaa0b9;
  --purple:#9d72ff;
  --red:#ff6f7d;
  --green:#6dd59a;
  --blue:#66c7ef;
  --border:rgba(245,199,108,.22);
  --safe-bottom:env(safe-area-inset-bottom, 0px);
}

*{box-sizing:border-box;-webkit-tap-highlight-color:transparent}
html,body{margin:0;min-height:100%;background:radial-gradient(circle at top,rgba(107,66,172,.28),transparent 34rem),var(--bg);color:var(--text);font-family:Georgia,"Times New Roman",serif}
body{min-height:100vh;min-height:100dvh}
button,select{font:inherit}
button{cursor:pointer}
.hidden{display:none!important}
.screen{display:none;min-height:100vh;min-height:100dvh}
.screen.active{display:block}

#start-screen{padding:max(30px,env(safe-area-inset-top)) 18px 34px}
.hero-banner{position:relative;overflow:hidden;max-width:800px;margin:0 auto 22px;padding:34px 22px;text-align:center;border:1px solid var(--border);border-radius:28px;background:linear-gradient(160deg,rgba(57,38,86,.96),rgba(17,11,27,.98));box-shadow:0 24px 70px rgba(0,0,0,.38)}
.hero-glow{position:absolute;width:240px;height:240px;left:50%;top:-125px;transform:translateX(-50%);border-radius:50%;background:rgba(245,199,108,.28);filter:blur(48px)}
.temple-icon{position:relative;font-size:58px}
.eyebrow{margin:0 0 7px;color:var(--gold);font-family:Arial,sans-serif;font-size:11px;font-weight:800;letter-spacing:.18em}
h1,h2,h3,p{margin-top:0}
h1{position:relative;margin-bottom:14px;font-size:clamp(42px,11vw,72px);line-height:.95}
h1 span{color:var(--gold)}
.intro,.muted,.city-card small,.hero-card p,.monster-card p,.quest-card p,.audience-card p,.log-card p,.summary-card p{color:var(--muted);font-family:Arial,sans-serif;line-height:1.45}
.start-card{max-width:800px;margin:auto}
.start-card h2{text-align:center}
.god-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}
.god-card{min-height:145px;padding:15px;color:var(--text);text-align:left;border:1px solid rgba(255,255,255,.08);border-radius:20px;background:linear-gradient(145deg,var(--panel-2),var(--panel))}
.god-card.selected{border-color:var(--gold);box-shadow:0 0 0 2px rgba(245,199,108,.15)}
.god-card .symbol{display:block;margin-bottom:8px;font-size:34px}
.god-card strong{display:block;color:var(--cream);font-size:19px}
.god-card small{display:block;margin-top:5px}
.primary-button,.secondary-button,.danger-button{width:100%;margin-top:16px;padding:16px;border-radius:16px;font-weight:800}
.primary-button{color:#251a08;border:none;background:linear-gradient(135deg,var(--gold),var(--gold-dark))}
.primary-button:disabled{opacity:.45}
.secondary-button{color:var(--gold);border:1px solid var(--gold);background:transparent}
.danger-button{color:white;border:1px solid rgba(255,111,125,.45);background:rgba(255,111,125,.12)}

.topbar{position:sticky;z-index:10;top:0;display:flex;align-items:center;justify-content:space-between;gap:10px;padding:max(14px,env(safe-area-inset-top)) 14px 11px;border-bottom:1px solid rgba(255,255,255,.06);background:rgba(13,8,22,.91);backdrop-filter:blur(18px)}
.topbar h2{margin:0;color:var(--cream);font-size:22px}
.top-actions{display:flex;align-items:center;gap:8px}
.icon-button{width:44px;height:44px;color:white;border:1px solid var(--border);border-radius:14px;background:var(--panel)}
.year-button{padding:11px 13px;color:#241904;border:none;border-radius:14px;background:linear-gradient(135deg,var(--gold),var(--gold-dark));font-family:Arial,sans-serif;font-size:12px;font-weight:900}

.resource-strip{display:grid;grid-template-columns:repeat(4,1fr);gap:1px;background:rgba(255,255,255,.06)}
.resource-strip div{padding:9px 4px;text-align:center;background:#15101f}
.resource-strip span,.resource-strip strong,.resource-strip small{display:block}
.resource-strip strong{margin:2px 0;color:var(--cream);font-family:Arial,sans-serif;font-size:14px}
.resource-strip small{color:var(--muted);font-family:Arial,sans-serif;font-size:9px;text-transform:uppercase}

#main-content{padding:18px 14px calc(98px + var(--safe-bottom))}
.view{display:none;max-width:860px;margin:auto}
.view.active{display:block}
.section-heading{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-bottom:14px}
.section-heading h2{margin:0;font-size:27px}
.pill{padding:7px 10px;color:var(--gold);border:1px solid var(--border);border-radius:999px;background:rgba(245,199,108,.08);font-family:Arial,sans-serif;font-size:11px;font-weight:800}
.summary-card,.city-card,.hero-card,.monster-card,.quest-card,.audience-card,.resource-card,.log-card{padding:16px;border:1px solid rgba(255,255,255,.08);border-radius:20px;background:linear-gradient(145deg,rgba(40,27,59,.97),rgba(24,16,37,.97));box-shadow:0 14px 28px rgba(0,0,0,.2)}
.summary-card{margin-bottom:12px}
.city-list,.stack{display:grid;gap:12px}
.card-row,.city-top,.hero-top,.monster-top,.choice-row,.stats-row{display:flex;align-items:center;justify-content:space-between;gap:10px}
.city-symbol,.portrait{font-size:34px}
.city-name,.hero-name{flex:1}
.city-name h3,.hero-name h3{margin-bottom:3px}
.stats{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin:14px 0}
.stat{padding:9px 7px;text-align:center;border-radius:12px;background:rgba(255,255,255,.04);font-family:Arial,sans-serif}
.stat strong,.stat small{display:block}
.stat small{margin-top:3px;color:var(--muted);font-size:9px;text-transform:uppercase}
.meter{height:8px;overflow:hidden;border-radius:999px;background:rgba(255,255,255,.08)}
.meter span{display:block;height:100%;border-radius:inherit;background:linear-gradient(90deg,var(--purple),var(--gold))}
.meter.red span{background:linear-gradient(90deg,var(--red),#ffad6a)}
.meter.green span{background:linear-gradient(90deg,var(--green),var(--blue))}
.influence-grid{display:grid;gap:7px;margin-top:12px}
.influence-line{display:grid;grid-template-columns:86px 1fr 34px;gap:8px;align-items:center}
.influence-line label,.influence-line strong{font-family:Arial,sans-serif;font-size:11px}
.influence-line strong{text-align:right}
.tag-row{display:flex;flex-wrap:wrap;gap:6px;margin:10px 0}
.tag{padding:5px 8px;border-radius:999px;background:rgba(255,255,255,.05);color:var(--muted);font-family:Arial,sans-serif;font-size:10px}
.tag.gold{color:var(--gold);border:1px solid var(--border)}
.card-button,.choice-button,.action-button{padding:11px 13px;color:var(--cream);border:1px solid var(--border);border-radius:12px;background:rgba(245,199,108,.08);font-family:Arial,sans-serif;font-weight:800}
.card-button:disabled,.choice-button:disabled,.action-button:disabled{opacity:.42}
.choice-row{align-items:stretch;margin-top:13px}
.choice-button{flex:1}
.choice-button.good{border-color:rgba(109,213,154,.35);background:rgba(109,213,154,.08)}
.choice-button.bad{border-color:rgba(255,111,125,.32);background:rgba(255,111,125,.08)}
.quest-difficulty{font-family:Arial,sans-serif;font-size:11px;font-weight:800;color:var(--gold)}
.resource-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}
.resource-card{text-align:center}
.resource-card .resource-icon{font-size:34px}
.resource-card strong{display:block;margin:7px 0 4px;font-family:Arial,sans-serif;font-size:24px;color:var(--cream)}
.resource-card small{color:var(--muted);font-family:Arial,sans-serif}
.income-box{margin-top:12px;padding:14px;border:1px solid var(--border);border-radius:16px;background:rgba(245,199,108,.06)}
.income-box h3{margin-bottom:8px}
.income-line{display:flex;justify-content:space-between;padding:6px 0;color:var(--muted);font-family:Arial,sans-serif;border-bottom:1px solid rgba(255,255,255,.05)}
.income-line:last-child{border-bottom:none}

.bottom-nav{position:fixed;z-index:20;right:0;bottom:0;left:0;display:grid;grid-template-columns:repeat(7,1fr);padding:7px 2px calc(7px + var(--safe-bottom));border-top:1px solid rgba(255,255,255,.08);background:rgba(17,11,27,.97);backdrop-filter:blur(18px)}
.nav-button{padding:5px 1px;color:var(--muted);border:none;background:transparent;font-family:Arial,sans-serif;font-size:8px}
.nav-button span{display:block;margin-bottom:2px;font-size:19px}
.nav-button.active{color:var(--gold)}

.modal{position:fixed;z-index:50;inset:0;display:flex;align-items:flex-end;justify-content:center;padding:16px;background:rgba(0,0,0,.74);backdrop-filter:blur(6px)}
.modal-card{position:relative;width:min(100%,760px);max-height:88vh;overflow-y:auto;padding:24px 18px 20px;border:1px solid var(--border);border-radius:24px;background:#1b132a}
.modal-close{position:absolute;top:12px;right:12px;width:38px;height:38px;color:white;border:none;border-radius:50%;background:rgba(255,255,255,.08)}
.action-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:10px;margin-top:14px}
.action-grid button{min-height:70px}
.battle-log{display:grid;gap:8px;margin:14px 0}
.battle-line{padding:10px;border-radius:12px;background:rgba(255,255,255,.04);font-family:Arial,sans-serif;color:var(--muted)}

.toast{position:fixed;z-index:80;right:16px;bottom:calc(96px + var(--safe-bottom));left:16px;max-width:520px;margin:auto;padding:13px 16px;text-align:center;border:1px solid var(--border);border-radius:14px;background:#2a1e3d;box-shadow:0 15px 45px rgba(0,0,0,.45);font-family:Arial,sans-serif;font-weight:700}

@media(min-width:720px){
  .god-grid{grid-template-columns:repeat(3,1fr)}
  .city-list{grid-template-columns:repeat(2,minmax(0,1fr))}
  .resource-grid{grid-template-columns:repeat(4,minmax(0,1fr))}
  .modal{align-items:center}
}
